{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Serve and Debug Jekyll (Full Workflow)",
      "type": "msedge",
      "request": "launch",
      "preLaunchTask": "Decrypt + Build + Serve",
      "url": "http://localhost:4000",
      "webRoot": "${workspaceFolder}",
      "postDebugTask": "Encrypt Posts"
    }
  ]
}
